"use strict"; // treat all JS code as  newer version
 
// alert("hello") // we are using NodeJS not browser


console.log(3 + 3); console.log("Ankush")  // this  code will run but it is not good for reading 


console.log(3 
    +
     4) // it will run but code readability should be high so it is a negative point 


     let name = "Ankush"
     let age = 18
     let isLoggedIn = true


     //number => 2 to power 52   
     // bigint => if more than  2power 52 tha we use 
     // string => " "  or ' '
     //boolean => true or false
     // null => standalone value 
     // undefined => when we declared variable but not declared value
     // symbol => unique
     //object => 

        console.log(typeof null); // when we are using null datatypes in object datatype so it  will comes object we can say error also 

        console.log(typeof undefined);  //undefined
        console.log(typeof null);   // object
